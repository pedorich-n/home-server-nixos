{% from "macros/oidc_entry.j2" import oidc_entry with context %}
{% import "macros/utils.j2" as utils with context %}

{% set redirect_uris = [ utils.generate_url("audiobookshelf") + "/auth/openid/callback",
                         utils.generate_url("audiobookshelf") + "/auth/openid/mobile-redirect" ] %}
{{ oidc_entry(name="Audiobookshelf", slug="audiobookshelf", application_group=applicationGroups.media, redirect_uris=redirect_uris, extra_property_mappings=["absgroups"]) }}
