{% from "macros/oidc_entry.j2" import oidc_entry with context %}
{% import "macros/utils.j2" as utils with context %}

{{ oidc_entry(name="Portainer", slug="portainer", application_group=applicationGroups.serverManagement, access_group=accessGroups.serverAdmins, redirect_uris=[ utils.generate_url("portainer") ] ) }}
