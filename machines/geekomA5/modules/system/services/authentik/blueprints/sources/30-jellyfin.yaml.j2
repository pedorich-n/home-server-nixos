{% from "macros/oidc_entry.j2" import oidc_entry with context %}
{% import "macros/utils.j2" as utils with context %}

{% set redirect_uris = [ utils.generate_url("jellyfin") + "/sso/OID/redirect/Authentik",
                         utils.generate_url("jellyfin") + "/sso/OID/r/Authentik" ] %}
{{ oidc_entry(name="Jellyfin", slug="jellyfin", application_group=applicationGroups.media, redirect_uris=redirect_uris) }}
