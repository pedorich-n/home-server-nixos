name: Nix Flake Check
on: push

jobs:
  nix-flake-check:
    uses: pedorich-n/github-workflows/.github/workflows/flake-check.yaml@rework-flows # main
    with:
      timeout_minutes: 20
    secrets:
      client_id: ${{ vars.APP_CLIENT_ID }}
      private_key: ${{ secrets.APP_PRIVATE_KEY }}
