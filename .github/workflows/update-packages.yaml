name: Update Packages' Sources

on:
  workflow_dispatch:
  schedule:
    - cron: "0 10 1 * *"

jobs:
  update-packages:
    runs-on: ubuntu-latest
    steps:
      - name: Get Github App info
        id: app_auth
        uses: pedorich-n/github-workflows/.github/actions/app-token@8dae1302115fad2af5d92f4e0368f727b17cd8ce # main
        with:
          client_id: ${{ vars.APP_CLIENT_ID }}
          private_key: ${{ secrets.APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}

      - name: Setup job
        uses: pedorich-n/github-workflows/.github/actions/setup-nix-job@8dae1302115fad2af5d92f4e0368f727b17cd8ce # main
        with:
          git_https_token: ${{ steps.app_auth.outputs.token }}

      - name: Get packages list
        id: get_packages
        run: |
          {
            echo 'packages<<EOF'
            nix eval --json .#nixUpdatePackages | jq -r '.[]'
            echo EOF
          } >> "$GITHUB_OUTPUT"

      - name: Run nix-update
        uses: pedorich-n/github-workflows/.github/actions/nix-update@8dae1302115fad2af5d92f4e0368f727b17cd8ce # main
        with:
          args: "--use-update-script --flake"
          token: ${{ steps.app_auth.outputs.token }}
          committer: ${{ steps.app_auth.outputs.committer }}
          author: ${{ steps.app_auth.outputs.committer }}
          packages: ${{ steps.get_packages.outputs.packages }}
