name: Update flake.lock
on:
  workflow_dispatch: # allows manual triggering
  schedule:
    - cron: "0 10 1 * *"

jobs:
  update-flake-lock:
    uses: pedorich-n/github-workflows/.github/workflows/update-flake-lock.yaml@4574wr905ea0c10728187de51f08d8729574413932 # main
    secrets:
      git_https_token: ${{ secrets.GH_ACCESS_TOKEN }}
